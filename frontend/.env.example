# In local dev, leave this empty — Vite proxies /api to localhost:5000 automatically.
# In production (Vercel), set this to your Railway backend URL (no trailing slash).
# Example: https://second-website-backend.up.railway.app
VITE_API_URL=
